<Application x:Class="PageMaker365.Installer.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="PmBackground" Color="#070A16" />
        <SolidColorBrush x:Key="PmPanel" Color="#11172A" />
        <SolidColorBrush x:Key="PmPanelStrong" Color="#19213A" />
        <SolidColorBrush x:Key="PmLine" Color="#2D3A66" />
        <SolidColorBrush x:Key="PmText" Color="#F7FBFF" />
        <SolidColorBrush x:Key="PmMuted" Color="#AEBAD0" />
        <SolidColorBrush x:Key="PmCyan" Color="#19D8E9" />
        <SolidColorBrush x:Key="PmBlue" Color="#1977FF" />
        <SolidColorBrush x:Key="PmViolet" Color="#8348EE" />
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="42" />
            <Setter Property="Padding" Value="18,10" />
            <Setter Property="Foreground" Value="{StaticResource PmText}" />
            <Setter Property="Background" Value="#1D2A4E" />
            <Setter Property="BorderBrush" Value="#415891" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#263765" />
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#5C76C8" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#182344" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#64708A" />
                                <Setter Property="Cursor" Value="Arrow" />
                                <Setter TargetName="Chrome" Property="Background" Value="#101726" />
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#26324D" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource PmText}" />
        </Style>
    </Application.Resources>
</Application>
